{% extends "base.html.jinja" %}

{% block title %}Смена пароля{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{{ url_for('static', filename='css/forms.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/auth.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/style_base.css') }}">
{% endblock %}

{% block content %}
<div class="main-content">
    <div class="auth-container">
        <div class="auth-card">
            <div class="auth-header">
                <h1 class="auth-title">🔐 Смена пароля</h1>
                <p class="auth-subtitle">Защитите свой аккаунт, {{ user.username }}</p>
            </div>
            
            <form method="POST" class="auth-form">
                {{ form.csrf_token }}
                
                <div class="form-group">
                    {{ form.current_password.label }}
                    {{ form.current_password(class="form-control", placeholder="Введите текущий пароль") }}
                    {% for error in form.current_password.errors %}
                        <span class="form-error">{{ error }}</span>
                    {% endfor %}
                </div>
                
                <div class="form-group">
                    {{ form.new_password.label }}
                    {{ form.new_password(class="form-control", placeholder="Введите новый пароль") }}
                    {% for error in form.new_password.errors %}
                        <span class="form-error">{{ error }}</span>
                    {% endfor %}
                </div>
                
                <div class="form-group">
                    {{ form.confirm_new_password.label }}
                    {{ form.confirm_new_password(class="form-control", placeholder="Подтвердите новый пароль") }}
                    {% for error in form.confirm_new_password.errors %}
                        <span class="form-error">{{ error }}</span>
                    {% endfor %}
                </div>
                
                <div class="form-actions">
                    {{ form.submit(class="button") }}
                </div>
            </form>
            
            <div class="auth-footer">
                <p><a href="{{ url_for('profile.view_profile') }}" class="auth-link">← Вернуться к профилю</a></p>
            </div>
        </div>
    </div>
</div>
{% endblock %}